services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SQLITE_DB_PATH: /data/optimizer.sqlite3
    volumes:
      - sqlite-data:/data

volumes:
  sqlite-data:
