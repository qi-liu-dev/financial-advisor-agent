services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      SQLITE_DB_PATH: /data/optimizer.sqlite3
      CORS_ALLOWED_ORIGINS: http://localhost:4200
    volumes:
      - sqlite-data:/data
    restart: unless-stopped

volumes:
  sqlite-data:
